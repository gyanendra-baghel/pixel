def generate_caption(image_path, image_id):
    # Dummy caption generator
    filename = image_path.split("/")[-1]

    # Simulate some processing time
    # request to add caption in the database
    return f"A photo named {filename}"
